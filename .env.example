# No secrets belong in browser/VITE_ variables.
# The setup script generates .dev.vars and .local/bridge.json with private permissions.
# To use another bridge config:
# HELD_CONFIG=/absolute/path/to/bridge.json npm run bridge
