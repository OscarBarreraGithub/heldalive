# Edition 05 — make the premise understandable

Oscar's correction: the public page must explain that the AI runs in browsers and stops when too many leave. The darker point is awareness of distributed AI and botnets. Remove Mac mini branding from the product, explain model download versus task input versus computation, and make helper sprites understandable.

## Decisions

- One clear headline/subtitle, a live 32-part completeness meter, and a precise stopped/running/preview status.
- “Death” means no further generation while coverage is missing; model files and saved work survive and can restart. No claim of consciousness or irreversible deletion.
- Preserve the consent boundary: this is disclosed, pausable browser participation, with no infection, self-spreading or arbitrary execution.
- Distinguish held model bytes, measured layer work in this tab, task inputs, and generated text. Show the actual task instructions on demand, including a queued task when stopped. Never call word counts token counts or cached model size network traffic.
- A helper is a temporary task using the same small model, executed by a complete group (or explicitly labeled preview support). It is not a model installed by the coffee button. Explain this beside the sprites.
- Keep the public premise truthful: the user was asked whether to retire launch support now. Pending reply, do not silently hide an active fallback or disable it based on silence. The page handles both modes with an explicit preview banner when support is on.

## Alien art direction (September 16)

- Oscar supplied `aliens.png`: white line art with alpha, visible on a dark background. Preserve the two inward-curled antennae, rounded head, long vertical eyes, domed saucer, paired hull marks and broad rim.
- Use a small hand-drawn pixel-grid SVG alien outside the ship, within an open cutaway saucer. It moves between its console, drawing station, memory library and stargazing window. The spacecraft is a place, not a handheld toy.
- Smooth travel, restrained floating/blinking, an antenna hello, and reduced-motion support. Miniature aliens remain real temporary helper jobs. Decoration must never masquerade as generated work.
- Carry the alien into the header, favicon, empty states and sharing card.

## Work

- [x] Main message, survival display, quiet stopped state, helper explanation.
- [x] Real local compute/model-size telemetry and readable task inputs.
- [x] Botnet/artwork explanation and consistent source wording throughout.
- [x] Verify actual telemetry/inputs, mode accuracy, mobile/accessibility, retained browser inference.
- [x] Publish/deploy and record the chosen operating mode and evidence.
